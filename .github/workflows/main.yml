name: Build and Publish Docker Image

on:
  push:
    branches:
      
main

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
      
name: Checkout repository
      uses: actions/checkout@v3

      
name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

      
name: Build and Push Docker image
      env:
        DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      run: |
        echo $DOCKER_PASSWORD | docker login ghcr.io -u $DOCKER_USERNAME --password-stdin
        docker buildx create --use
        docker buildx build --platform linux/amd64 -t ghcr.io/username/repo:${{ github.sha }} .
        docker buildx build --platform linux/arm64 -t ghcr.io/username/repo:${{ github.sha }} .
        docker push ghcr.io/username/repo:${{ github.sha }}




git remote add origin https://github.com/Droidenn/Test-4-.git
git branch -M main
git push -u origin main
